# Build roadmap & progress

Tracks progress against `docs/Khata_app-Guide.pdf` sections 8 (user stories) and 10 (phase plan). Update the status column as phases complete — check a phase's acceptance criteria (AC) in the PDF before marking it done, not just that screens exist.Update this file at the end of every phase/story you complete, as part of
the same commit. Keep entries short — this is a log, not documentation.

## Status legend
- [ ] not started · [~] branch created, in progress · [x] merged to main & branch deleted

Each story below should note its branch name once created, e.g.:
`[x] Customer list — search, sort, balance display (feature/1-b1-customer-list)`

| Phase | Scope | Stories | Status |
|---|---|---|---|
| 0 — Foundation | Expo+TS init, nav shell (tabs+stacks), theme tokens, i18next EN/UR + RTL flip, SQLite/Drizzle schema (5 tables), typed repository layer, seed data (3 demo customers) | none (skeleton) | ✅ Done |
| 1 — Customers | Searchable customer list (name/phone, live search, sort by balance/recency), add/edit customer form, archive with audit log | B1, B2, B3 | ✅ Done |
| 2 — Entries & running balance | Customer khata screen (color-coded balance header, newest-first entry list), simple cash-in/cash-out entry, home dashboard totals | C1, C2, D2 | ✅ Done |
| 3 — Itemized bills | Bill form (line items, size/color pickers, garment palette, quantity/rate/amount), auto-description engine, live bill total. **The centerpiece — budget the most time here.** | C3 | ✅ Done |
| 4 — Edit, delete & history | Edit/soft-delete any entry with audit_log diff, entry history timeline | C4, C5 | ✅ Done |
| 5 — Sharing & export | WhatsApp/SMS share (bill/statement text + deep link), PDF export (`expo-print`), Excel/CSV export (`xlsx` + `expo-file-system`) | D1, D3 | ✅ Done |
| 6 — Security & onboarding | PIN + biometric app lock (`expo-secure-store` + `expo-local-authentication`), first-run setup wizard, Settings screen, runtime light/dark/system theming | A1, A2 | ✅ Done |
| 7 — Polish & release | Empty states, error handling, large-number formatting, accessibility, app icons/splash, EAS store builds, full QA against every AC in section 8 | all | 🔶 In progress |

## Definition of done (v1, spec section 12)
- [ ] Every user story in spec section 8 passes its acceptance criteria.
- [ ] Balances always equal the recomputed sum of entries — never drift.
- [ ] Every create/edit/delete on a customer or entry appears in the audit log.
- [ ] The app opens, adds a customer, records a bill, and shares it — fully offline.
- [ ] Switching to Urdu flips the entire UI to RTL with translated strings.
- [ ] Money never shows floating-point rounding errors.
- [ ] Nothing is reachable without passing the PIN/biometric lock.



## Phase 1 — Customers (Stories B1–B3)
- [x] Customer list — search, sort, balance display (feature/1-b1-customer-list, 2026-07-24)
- [x] Add/edit customer form (feature/1-b2-add-edit-customer, 2026-07-24)
- [x] Archive (soft-hide) (feature/1-b3-archive-customer, 2026-07-24)
- [x] Balance computed from entries (listCustomersWithBalance, feature/1-b1-customer-list, 2026-07-24)

## Phase 2 — Simple entries & running balance (Stories C1, C2, D2)
- [x] Customer khata screen (feature/2-c1-c2-d2-khata-entries, 2026-07-24)
- [x] Simple cash-in / cash-out entry form (feature/2-c1-c2-d2-khata-entries, 2026-07-24)
- [x] Running balance math (computeBalanceFromEntries, reused from Phase 0/1, feature/2-c1-c2-d2-khata-entries, 2026-07-24)
- [x] Home dashboard totals (feature/2-c1-c2-d2-khata-entries, 2026-07-24)

## Phase 3 — Itemized bills (Story C3)
- [x] Bill form / line-item editor (feature/3-c3-itemized-bill-form, 2026-07-25)
- [x] Size & color pickers (garment palette) (feature/3-c3-itemized-bill-form, 2026-07-25)
- [x] Auto-description engine (+ description_touched override) (feature/3-c3-itemized-bill-form, 2026-07-25)
- [x] Live bill total, multi-line support (feature/3-c3-itemized-bill-form, 2026-07-25)
- [x] Bill created from a customer's khata (entries list) screen — "+ New Entry" now offers Simple entry vs Itemized bill, and the created entry lands in that customer's entry list (feature/3-c3-itemized-bill-form, 2026-07-25)
- [x] Read-only bill/entry detail view (line items, total, note, attachment) — Edit/Delete/Share/History actions still land in Phases 4–5 (feature/3-c3-itemized-bill-form, 2026-07-25)
- [x] Bill form UX refinement — screen retitled "New Bill"; only one line-item form is open at a time, completed lines collapse to a compact tappable summary row (tap to reopen), and each line's description is folded into the bill note (newline-separated, appended after any text the user already typed) (feature/3-c3-bill-ui-improvements, 2026-07-25)

## Phase 4 — Edit, delete & history (Stories C4, C5)
- [x] Edit entry (reopens correct form type — simple form or the bill line-item editor, pre-filled) (feature/4-c4-edit-entry, 2026-07-25)
- [x] Per-line-item audit diffs on bill edits (create/edit/delete logged per line, matched by id) (feature/4-c4-edit-entry, 2026-07-25)
- [x] Soft delete (`deleteEntry`, isDeleted flag, never a real DELETE) (feature/4-c5-delete-history, 2026-07-25)
- [x] Audit log wired into every mutation (create/edit/delete on entries and line_items) (feature/4-c4-edit-entry, feature/4-c5-delete-history, 2026-07-25)
- [x] Entry history / audit timeline screen — merges the entry's own audit trail with its current line items' trails, newest first (feature/4-c5-delete-history, 2026-07-25)
- [x] `EntryDetailScreen` now has Edit / History / Delete actions — Share is still Phase 5 (feature/4-c4-edit-entry, feature/4-c5-delete-history, 2026-07-25)

## Phase 5 — Sharing & export (Stories D1, D3)
- [x] WhatsApp/SMS share (bill & statement text + deep link, generic-share fallback) (feature/5-d1-share-pdf, 2026-07-25)
- [x] PDF export (single bill + full statement) via `expo-print` — receipt styled after the reference bill layout (feature/5-d1-share-pdf, 2026-07-25)
- [x] Share action on `EntryDetailScreen` (bill/receipt) + "Share statement" header action on `CustomerKhataScreen` (feature/5-d1-share-pdf, 2026-07-25)
- [x] CSV export (feature/5-d3-excel-csv-export, 2026-07-25)
- [x] Excel (.xlsx) export — Customers + Entries sheets, via `xlsx` + `expo-file-system/legacy` (feature/5-d3-excel-csv-export, 2026-07-25)
- [x] Real Reports screen (totals, customer/entry counts, CSV/Excel export buttons) (feature/5-d3-excel-csv-export, 2026-07-25)

## Phase 6 — Security & onboarding (Stories A1, A2)
- [x] Runtime light/dark/system theming — `ThemeContext`/`useTheme()`, `lightColors`/`darkColors` in `src/theme/colors.ts`, `settings.themeMode` column + migration `0001`, every screen refactored to `makeStyles(colors)` (feature/6-theme-mode, 2026-07-25)
- [x] PIN + biometric lock, lockout after failed attempts — `src/lib/appLock.ts` (salted SHA-256 in `expo-secure-store`, 5 attempts / 60s lockout), `LockScreen` (PIN pad, auto biometric prompt), re-locks on every background→active transition (feature/6-a1-a2-security-onboarding, 2026-07-25)
- [x] First-run setup wizard — `OnboardingScreen` (business profile + language → appearance → optional PIN + biometric), gates the app outside the tab navigator until `setOnboarded()` (feature/6-a1-a2-security-onboarding, 2026-07-25)
- [x] Settings screen (profile, language, theme mode, PIN set/change/remove, biometric toggle, backup/restore) — full build replacing the placeholder (feature/6-a1-a2-security-onboarding, 2026-07-25)
- [x] Backup/restore — full-ledger JSON export (share sheet) and restore; restore is Android-only (Storage Access Framework directory pick + fixed filename, since `expo-document-picker` isn't a dependency and adding it would need a dev-client rebuild) (feature/6-a1-a2-security-onboarding, 2026-07-25)

Known scope notes for Phase 7 pickup:
- PIN setup is optional during onboarding (skippable, matching spec's "first-run wizard" rather than a forced gate) — the lock screen only appears once a PIN actually exists. `settings.pinHash` (the DB column from the original Phase 0 schema) is unused; the real PIN hash lives only in `expo-secure-store` via `src/lib/appLock.ts` — don't write to `settings.pinHash`.
- Restore on iOS shows an "unavailable" message — there's no SAF equivalent there. A real cross-platform restore needs `expo-document-picker` (new native dependency, dev-client rebuild).
- Switching language mid-session still needs an app restart to fully flip RTL layout (same `I18nManager.forceRTL` constraint as before) — Settings shows a restart alert rather than auto-reloading, since `expo-updates` isn't a dependency.
- `app.json`'s `userInterfaceStyle` was changed from `"light"` to `"automatic"` to match the new system theme option — this only takes full effect after the next native rebuild (`expo prebuild` / `expo run:android`), not on the already-built dev client.

## Phase 7 — Polish & release (feature/7-polish-release, in progress)
- [x] Error handling — every screen's data-loading effect and mutation handler now wraps in try/catch/finally and surfaces `common.errorTitle`/`common.errorMessage` via `Alert` on failure, instead of letting a rejected promise fail silently
- [x] Empty states — audited every list-like screen (`CustomerListScreen` incl. new archived-filter empty state, `CustomerKhataScreen`, `EntryHistoryScreen`, `HomeScreen` today's-activity, `ReportsScreen` incl. new no-entries-in-range state); all have a real translated empty message, none render a blank screen
- [x] Large-number formatting — `formatMoney` already grouped via `toLocaleString("en-IN")`; added `numberOfLines={1}` (+ `adjustsFontSizeToFit` on the largest headline totals) to every money-displaying `Text` so a big balance can't wrap or overflow its card
- [x] Accessibility pass — every `Pressable` in `src/` now has `accessibilityRole="button"` (plus `accessibilityLabel`/`accessibilityState` where the visible text doesn't already say enough, e.g. icon-only buttons, swatches, chips), and both `Switch`es (biometric toggle in Onboarding + Settings) have `accessibilityLabel`
- [x] App icon/adaptive icon/favicon redesigned (navy `#22335B` + gold, document+checkmark mark) — `app.json` updated (`name: "Khata"`, adaptive icon background color); splash screen still uses Expo's default icon-based splash (no `expo-splash-screen` dependency added)
- [x] `eas.json` added (development/preview/production build profiles, `appVersionSource: "local"`) — actually running `eas build`/`eas submit` needs the user's Expo account and is not something this agent can do
- [x] Customer archive UX closed the loop — `CustomerFormScreen` now supports **unarchive** (was archive-only), `CustomerListScreen` has an "Archived" filter chip + empty state, and tapping an archived row now opens the edit form (where Unarchive lives) instead of the khata screen
- [x] `CustomerKhataScreen` can now reveal soft-deleted entries (`khata.showDeleted`/`hideDeleted` toggle) — tapping a deleted row opens its history instead of the (now soft-deleted) detail screen
- [x] Phone number validation on `CustomerFormScreen` (`customerForm.phoneInvalid`) — loose but real check (7–15 digits), optional field
- [x] Reports date-range filter (`reports.dateRange`/`dateFrom`/`dateTo`/`clearFilter`) — the item `ui.md`'s Reports row had deferred to Phase 7; filters both export buttons and the on-screen entry count
- [x] App-lock re-lock timing changed from "every background→active transition" to a `RELOCK_AFTER_MS` (2 min) threshold, and `LOCKOUT_MS` changed from 60s to 30s, to match spec A2's AC on brief backgrounding (e.g. picking a photo, opening the share sheet) not re-triggering the PIN
- [x] Static QA pass against every AC in spec Section 8 (read the PDF, cross-checked each story's AC line-by-line against the actual repository/screen code) — found and fixed two real gaps, both below (D2, D3). Everything else in Section 8 (A1, A2, B1–B3, C1–C5, D1) checked out against the code as written.
- [x] **D2 fix** — "Tapping a stat filters the list" wasn't actually true before this pass (it only sorted by balance, mixing receivable and payable customers). `HomeScreen`'s two totals now navigate with `{ initialSort: "balance", balanceFilter: "receivable" | "payable" }`; `CustomerListScreen` filters by balance sign and shows a dismissible chip (`customers.filterReceivable`/`filterPayable`) plus dedicated empty states. Along the way, fixed a real bug this would have hit: since React Navigation reuses an already-mounted screen instance and just updates `route.params`, the old `useState(route.params?.x ?? default)` pattern (also used for `initialSort`) would go stale on a second stat tap — now synced via a `useEffect` keyed on `JSON.stringify(route.params)`.
- [x] **D3 fix** — the per-customer PDF/text statement ("Share statement" on `CustomerKhataScreen`) was whole-history only; spec D3 explicitly calls for "date-ranged". `getStatementDocumentData` (`documentRepository.ts`) now takes optional `{ dateFrom, dateTo }`, computing the period's effective opening balance from everything before `dateFrom` so the in-range running balance stays continuous; `buildStatementHtml`/`buildStatementText` show a "Period" line when ranged. `CustomerKhataScreen` has a collapsed "Filter statement by date" control (same `isValidDate` pattern as the Reports date filter) that scopes only the share action, not the visible entry list. Note: the Reports screen's *separate* date-range filter (added earlier this phase) is unrelated — it scopes the bulk CSV/Excel export, not this per-customer statement.
- [ ] EAS store builds — `eas.json` profiles exist but no build has actually been run/submitted (needs the user's Expo account + is a billed/external action)
- [ ] Full **on-device** QA pass against spec Section 8 — the static/code-level QA above is done, but no manual on-device pass has happened this session; see `project_device_adb_restrictions` memory (adb input injection is blocked on the test device, so this needs a human tester, not just `am start`/`screencap`)
- [x] **UI redesign against `design/` reference images** (feature/7-ui-redesign, 2026-07-26) — every screen restyled to match its own reference image, no data-model/repository/navigation/business-logic changes. See `.claude/docs/ui.md` (Design tokens / Header convention / Known gaps sections, all new this pass) for the concrete color/typography/spacing values and the shared components this introduced (`Avatar`, `StatCard`, `ActionSheet`). Restyled: Onboarding (3 steps, incl. a real digit-keypad PIN entry replacing the old two-`TextInput` pair), Lock screen (now a fixed navy identity independent of theme, both normal and lockout states), Home/Dashboard, Customer list (incl. true-empty and no-search-results states with icon+title+description+CTA), Add/Edit customer, Customer khata (navy header + translucent balance banner, `ActionSheet`-based New Entry and Share sheets), New Entry — Simple, New Bill + line-item editor, Entry detail (incl. its Share sheet), Entry history (audit timeline), Reports (minus the bar chart, see below), Settings (grouped sections, business avatar, currency quick-pick chips). `src/theme/colors.ts` gained `primarySoft`/`owesMeSoft`/`iOweSoft` (pixel-sampled soft-tint tokens) and a `withAlpha()` helper; the existing spacing/radius/typography tokens already matched the reference sheet exactly and needed no changes.
  - Not implemented, flagged rather than faked: Reports' "Receivable vs collected" bar chart (needs a new weekly-aggregation query + a charting dependency, not just layout — see `ui.md`); the Simple Entry calculator-style keypad (amount stays a plain direction-coloured `TextInput`, avoiding a hand-rolled expression evaluator around money math); Home's decorative-only "See all" link (no destination screen exists) and notification bell (no notifications feature).
  - Verification: `npx tsc --noEmit` clean, `npx expo-doctor` 20/20, `npx expo export --platform android` bundles clean (1256 modules), every Ionicons glyph name used verified against the installed glyph map.
- [x] **Customer list / Add-Edit customer pixel-accuracy pass + customer delete** (2026-07-26) — re-matched `CustomerListScreen`/`CustomerFormScreen` against `design/06`, `06b`, `07` by pixel-sampling (not eyeballing): FAB/CTA/Save/Archive buttons corrected from `radius.md`/`pill` to `radius.lg` (a squircle, not a circle or stadium — confirmed by cropping the FAB and button corners); search bar to `radius.pill`; empty-state icon glyph and the new empty-add-customer avatar placeholder to `colors.primaryMuted` (not `primary`); form field labels to `colors.textSecondary` with inline red `required`/gray `(optional)` markers; focused-input `colors.primary` border; opening-balance suffix reworded to match ("owes you"/"you owe" vs. the longer `khata.*` sentences). Search placeholder and Archive/Unarchive button text corrected to match the reference copy exactly ("Search name or phone", "Archive customer").
  - Added real customer delete: `customerHasEntries()`/`deleteCustomer()` (`customerRepository.ts`) permit a **permanent** delete only when a customer has zero entries (documented as the one exception to "deletes are soft" in `architecture.md`/`data-model.md`) — falls back to the existing `setCustomerArchived()` soft-delete otherwise. Wired into `CustomerFormScreen` (single Delete-or-Archive button, switching on `hasEntries`/`isArchived`) and a new long-press context menu on `CustomerListScreen` rows (`ActionSheet`: Edit / Delete).
  - Added `ConfirmDialog` (`src/components/`) — a themed centered confirmation modal matching the app's design tokens, replacing `Alert.alert` for the archive/unarchive/delete confirmations specifically (plain error alerts elsewhere are untouched, see `ui.md`).
  - Verification: `npx tsc --noEmit` clean, plus pixel-cropping/color-sampling every restyled element against its `design/` reference image. On-device screenshot verification was attempted but dropped this session — the physical test device rejected every reinstall attempt (`adb install`, `expo run:android`, twice, after clearing app data via `adb uninstall`) with `INSTALL_FAILED_USER_RESTRICTED` and no visible on-device prompt; confirmed a hard, repeatable device-level block, not a fluke. See `project_device_adb_restrictions` memory. A live on-device pass against this work is still owed whenever the device's install restriction gets resolved.
- [x] **Removed demo seed data** (2026-07-26) — `App.tsx` no longer calls a seeding step on boot; `src/db/seed.ts` (the Phase 0 "3 demo customers" seeder, `seedDemoData()`) is deleted outright. A fresh install now genuinely starts with zero customers, matching `design/06b`'s true-empty state — Phase 0's original "seed data" scope note above is superseded by this.
- [x] **Entry flow pixel-accuracy pass: khata ledger, Simple entry calculator, New Bill, Bill saved, Entry detail** (2026-07-26) — re-matched against `design/08`, `08b`, `10`, `10b`, `11`, `11b`, `12`. Supersedes the "Simple Entry calculator-style keypad" item in the Known-gaps note above — it's now real, not flagged out:
  - **Real calculator** (`src/lib/calculator.ts`, pure/testable, no UI deps) backs the Simple entry amount field — chain-style four-function calculator (+ − × ÷ %, matching a physical calculator's left-to-right evaluation, not scientific precedence) operating entirely in integer paisa (`Math.round` only at the two operations — × and ÷ — that mathematically require it; never a stored float). `CalculatorKeypad.tsx` (`src/screens/Entries/`, used only by `EntryFormScreen`) renders the C/⌫/%/÷/×/−/+/=/digit grid from `design/10`; the running-expression line and big result live-update off `calculator.ts`'s pure state.
  - **`DateField`** (`src/components/DateField.tsx`) — a shared calendar-picker component wrapping `@react-native-community/datetimepicker` (new dependency, `npx expo install`'d for SDK 57 compat), defaulting to today, replacing every free-text `YYYY-MM-DD` date `TextInput` in the app (khata's statement-range filter, Simple entry, New Bill).
  - **New Bill** (`design/11`): the always-open active line-item card now gets a visible `colors.primary` 2px border (was unstyled — the design shows it clearly outlined against the collapsed rows above it), an "Auto: '...' · tap to edit" caption under the description field when it's still auto-generated, and a "✨ Auto from all items" hint next to the Note label.
  - **Bill saved confirmation** (`design/11b`, new) — `BillSavedScreen.tsx` + `BillSaved` route (`CustomersStackParamList`). Creating a new bill now `navigation.replace()`s here instead of `goBack()` (edits still `goBack()` — this screen is specifically for a freshly-created bill). Shows the itemized recap, "This bill total", and a previous-balance → new-balance box; "View khata" goes back, "Share this bill" opens the same WhatsApp/SMS/PDF `ActionSheet` as Entry detail. Previous/new balance are computed via `computeCustomerBalance()` after the (already-committed) create, not passed through navigation params, so the screen is a normal fetch-on-mount screen like every other detail screen.
  - **Entry detail** (`design/12`): the Share/Edit/History/Delete action row moved out of the scrolling content into a fixed footer bar (was in-flow, scrolling away with the content); the header title is now dynamic ("Bill detail" vs "Entry detail" per `entry.type`, was a static generic "Entry").
  - **Customer khata** (`design/08`/`08b`): entries are now grouped under date section headers (`SectionList`, uppercased "15 JULY 2026" style) instead of a flat list; a synthetic, display-only "Opening balance" row appends after all real entries when `customer.openingBalance !== 0` (no schema/data change — purely derived from the existing field, same as the balance itself); the true-empty state (0 entries, 0 opening balance) now gets the full icon+title+description treatment matching `design/08b` ("No entries yet" / "Record the first bill or cash entry for {name}..." / bottom button relabeled "Add first entry"); the balance banner label changed to match the reference copy exactly ("Customer owes you" / "You owe customer" / "Balance") via new `khata.balanceOwesYou`/`balanceYouOwe`/`balanceZero` keys — the pre-existing `khata.theyOweYou`/`youOweThem`/`settled` keys are untouched and still used by `CustomerFormScreen`'s balance preview.
  - No data-model, repository, audit-log, or balance-calculation changes — `computeBalanceFromEntries`, `logAudit`, and the description auto-gen/note-combining logic are all unchanged and still exercised exactly as before.
  - Verification: `npx tsc --noEmit` clean, `npx expo-doctor` 20/20, `npx expo export --platform android` bundles clean (1275 modules), every new Ionicons glyph verified against the installed glyph map.
- [x] **Onboarding pixel-accuracy pass, PDF document redesign, app icon/splash, whole-rupee money display** (2026-07-26) — re-matched against `design/01`–`03`, `05c`, `17`, `17b`, `18`, `App Icon.png`:
  - **Onboarding** (`design/01`–`03`): step 1 gained the missing navy/gold book icon box above the title; content now offsets by `useSafeAreaInsets().top` so it no longer renders under the phone status bar; currency chips are flat `colors.surface` (were bordered/transparent) and the shared `LanguageSelector` (also used by Settings) now matches the reference's selected-chip treatment (`background` + `primary` border, like a focused input) instead of a solid navy fill; the biometric row is now a `colors.surface` card with a fingerprint icon, pushed toward the bottom like the reference. Fixed a real bug found while matching `design/03`: tapping "Skip" on the last (PIN) step was a no-op (it jumped to `STEP_COUNT - 1`, i.e. itself) — it now finishes onboarding without setting a PIN, via a new `finish({ skipPin: true })` path that bypasses PIN validation entirely rather than trying to clear PIN state first (which would have raced React's async `setState`).
  - **PDF bill/receipt and ledger statement** (`design/17`, `17b`, `src/lib/documentFormat.ts`): both documents rebuilt around a full-bleed navy header banner (business identity in white/gold on navy, was a bordered white header) instead of a bottom-border header; party-label text corrected to `textSecondary` gray (was `primary`); item swatches are now circles (was a 2px-radius square); the bill's previous-balance/balance-owed box is now two side-by-side cards (was one stacked box with an extra "this bill" line the reference doesn't show — the two shown numbers already reconcile: previous + bill total = balance owed). The statement gained real debit/credit color coding (red/green, matching the reference) and "Total debits"/"Total credits" boxes, renamed columns (Date/Description/Debit/Credit/Balance) and doc-type ("LEDGER STATEMENT"), and its party boxes changed from Customer/Ledger to Account/Statement-period. Both footers are now a two-column thanks-text + "Generated by Khata · {date}" line. `shortRef()` gained a `KH-`/`KH-LS-` prefix to match the reference's document numbers. No discount line was added (no such field exists in the data model — bills are qty×rate line items only); `buildBillText`/`buildStatementText` (the WhatsApp/SMS plain-text share messages) are unchanged. `documentStyles()`'s font sizes were also bumped across the board (body 12px→15px, business name 20px→26px, table cells 12px→16px, bill-total/balance amounts 16px→20-22px, etc.) after the first pass proved hard to read as an actual printed page — the earlier screenshot preview had looked fine only because it was rendered with an artificial `--force-device-scale-factor=1.5`, which flattered the true (too-small) proportions; verified afterward at a true 1:1 scale factor. Verified by rendering both builders through the existing esbuild+headless-Edge screenshot technique (see `project_html_pdf_preview_technique` memory) and comparing pixel-for-pixel against `design/17`/`17b`.
  - **App icon / adaptive icon / favicon / splash** (`design/App Icon.png`, `design/18`): the icon glyph changed from a document+checkmark mark to the open-book glyph (Ionicons `book`, the same one already used in-app for Onboarding step 1 and Home's empty state) on a navy gradient tile with the gold `accent` color, generated by rendering an HTML/CSS composition through headless Edge (`--default-background-color=00000000` for the transparent adaptive-icon foreground/monochrome layers) rather than hand-drawing raster assets — `assets/icon.png`, `favicon.png`, `android-icon-foreground.png`, `android-icon-monochrome.png` were regenerated this way; `android-icon-background.png` (solid navy) was already correct and untouched. Added the `expo-splash-screen` dependency + config plugin (none existed before — the app previously used Expo's zero-config default splash); `assets/splash-icon.png` is a new composed image (white icon card + navy book glyph + faint-gray "Khata" wordmark + light-blue tagline, matching `design/18`), cropped tight to its content bounding box so the plugin's `imageWidth: 240` scales correctly instead of measuring mostly-transparent padding.
  - **Whole-rupee money display** (`src/lib/money.ts`): `formatMoney` now omits the `.00` for whole-rupee amounts (the overwhelming majority) and only shows a decimal when a real paisa remainder exists — matches every reference image, none of which show a trailing `.00`. `formatMoneyInput`/`parseMoneyInput` (the editable-field round-trip) are unchanged, as is `exportData.ts`'s use of `formatMoneyInput` for spreadsheet cells.
  - **Home no-activity state** (`design/05c`): upgraded from plain centered text to the icon+title empty-state treatment already used elsewhere (`colors.primarySoft` icon box, `colors.primaryMuted` glyph) — this was previously called out in `ui.md`'s Known-gaps note as deferred for lack of a reference image; that note is now resolved.
  - **Dark-mode icon-on-chip contrast fix**: `darkColors.primarySoft` and `darkColors.primaryMuted` were both `#3A4A63` — identical — so every "icon glyph colored `primaryMuted` inside a `primarySoft` chip" pattern (`HomeScreen`'s new empty state, `CustomerListScreen`'s `EmptyState`, `CustomerKhataScreen`'s empty state, `CustomerFormScreen`'s empty-avatar placeholder) rendered a genuinely invisible icon in dark mode — same color as its own background. Fixed by changing only `darkColors.primarySoft` to `#242B38` (a distinct, darker tint); `darkColors.primaryMuted` was deliberately left untouched because `LockScreen.tsx` reads it directly (`LOCK_MUTED`) for its keypad-key/icon-box background — that screen is a fixed navy identity independent of theme (see `ui.md`) and must not change appearance.
  - **Input-field decimals**: `formatMoneyInput` (`src/lib/money.ts`, used to pre-fill the opening-balance and bill-rate `TextInput`s) now drops the `.00` for whole-rupee amounts too, same rule as `formatMoney` — editing a ₹6,500 opening balance now shows "6500" instead of "6500.00". `parseMoneyInput` (unchanged) still accepts a typed decimal if the user needs a fractional amount. `calculator.ts` already had a `.replace(/\.00$/, "")` workaround for the old always-decimal behavior — now redundant (matches on nothing when there's no `.00` left to strip) but harmless, left as-is.
  - Verification: `npx tsc --noEmit` clean, `npx expo-doctor` 20/20, `npx expo export --platform android` bundles clean (1277 modules).
- [x] **Onboarding layout fix — fixed header/footer, no-button PIN step** (2026-07-27) — the previous pass fixed status-bar overlap on step 1 only; step 2/3 still put the progress+Skip row and the Back/Next buttons inside the same `ScrollView` as the form content, so the buttons floated right after the fields instead of anchoring to the screen bottom like `design/01`–`03` show. `OnboardingScreen.tsx` restructured into three flex siblings instead of one scrolling column: a fixed header (progress dots + Skip, `insets.top`-padded), a middle `ScrollView` for the step content, and a fixed bottom bar (`insets.bottom`-padded) for the Back/Next buttons — this also fully closes out the "content renders under the status bar" concern since the header is a layout sibling, not an overlay, so scrolling can never move it. Step 3 (PIN) no longer has a Next/Finish button at all, matching `design/03` exactly: entering a matching PIN now auto-finishes onboarding the same way a correct PIN auto-unlocks `LockScreen` (a `useEffect` keyed on the derived `pinConfirmed` boolean calls `finish()` directly), and the "Unlock with biometrics" toggle is now shown in the fixed bottom bar for the whole PIN step (both entry and confirm stages) instead of only after the PIN matched. If `finish()` throws, the PIN entry is now reset (`clearPinEntry()`) so the user isn't stuck on a dead confirmed-but-failed state. Copy for `onboarding.pinHint`/`onboarding.enableBiometric` updated in both `en.json`/`ur.json` to match the reference wording exactly ("Set a 4-digit PIN. You can skip and add it later." / "Unlock with biometrics"). No changes to money formatting — `formatMoney`/`formatMoneyInput` (`src/lib/money.ts`) already dropped `.00` for whole-rupee amounts and only show a decimal on a real paisa remainder, from the prior pass; verified still correct everywhere (`calculator.ts`, `BillLineItemCard`, `CustomerFormScreen`) rather than re-touched. App icon/splash (`design/App Icon.png`/`18`) were also already in place from the prior pass, wired via `app.json`'s `expo-splash-screen` plugin and `android.adaptiveIcon` — confirmed still present, not regenerated.
  - Verification: `npx tsc --noEmit` clean, `npx expo-doctor` 20/20, `npx expo export --platform android` bundles clean.
- [x] **Entry history bug fix — deleted line items now show up in the audit timeline** (2026-07-27) — `EntryHistoryScreen` previously derived which line items to fetch audit rows for by mapping over `entry.lineItems` (the entry's *current* line items). Since a removed line item is hard-deleted from `line_items` on a bill edit (the documented exception to soft-delete — see `data-model.md`), a deleted line's id was never in that list, so its create/edit/delete audit rows were silently never fetched or shown — the timeline looked like the item had never existed. Root cause fixed at the schema level: `audit_log` gained a nullable `entryId` column (migration `0002_huge_komodo.sql`), populated on every `entity: "line_item"` audit row in `entryRepository.ts` (`createEntry`/`updateEntry`). `listLineItemAuditForEntry(db, entryId)` (`auditRepository.ts`) now queries by that column directly, independent of whether the line item row still exists. Also gave the line-item "delete" audit row an actual `diff` (itemName/quantity/amount → null) — it previously logged no diff at all, so a deleted line's history entry showed no detail on what was removed. Verified `dateFrom`/`dateTo` filters on `ReportsScreen` and `CustomerKhataScreen`'s statement share (both already default to `""` and treat empty as "show/include everything") were unaffected and already correct — no changes needed there.
  - Verification: `npx tsc --noEmit` clean, `npx drizzle-kit generate` produced the expected single-column migration.
- [x] **Khata date-filter rework, overflow menu, search-icon expand, midnight date-bug fix, responsive pass** (2026-07-27, feature/7-ui-redesign) — user-requested pass, not a spec story:
  - **Khata date filter** (`CustomerKhataScreen`, matching `design/08b`): replaced the plain From/To collapsible with preset chips (This month / Last 30 days / All time / Custom) plus Reset/Apply buttons (`statementPreset`/`appliedStatementRange` state; a pending selection only takes effect on Apply, Reset clears back to All time immediately; picking a date directly in a `DateField` switches the preset to Custom). **Supersedes the D3-fix note above**: per direct user feedback the filter now scopes both the on-screen entry list and the Share/Export output, not just the share text — an explicit product decision overriding that earlier "share-only" scope.
  - **Flat entry list with inline dates, replacing date section headers** — also supersedes the "Entry flow pixel-accuracy pass" note above, which had grouped entries under `SectionList` date headers; reverted to a flat `FlatList` per user feedback that the date should read within the entry, not as a separate header. Each `EntryRow` now shows `{shortDate} · {timeOfDay}` inline instead of the old relative-time-only label. Added a distinct empty state (icon + `khata.noEntriesInRange` title + description) for "filter applied, zero results in range", separate from the true zero-entries empty state.
  - **Overflow ("⋮") menu on `CustomerKhataScreen`** — added alongside the existing Share icon: Edit customer (→ `CustomerForm`), Export statement (new `ActionSheet` sub-choice: CSV or Excel, via a new `getCustomerExportData()` in `exportRepository.ts` — same `buildEntriesCsv`/`buildWorkbookBase64` builders as Reports' bulk export, scoped to one customer), Archive/Unarchive customer, and Delete customer — the latter two reuse `CustomerFormScreen`'s exact `ConfirmDialog` copy and the zero-entries-permanent-delete-else-archive logic already established in `CustomerListScreen`'s long-press menu.
  - **Search-icon expand on `CustomerListScreen`** (matching `design/06c`) — the always-visible search bar was replaced with a search icon next to the "Customers" title; tapping it expands to a focused full-width search row with a back arrow (collapses and clears on tap). While searching, matched substrings in customer names are highlighted (`withAlpha(colors.accent, 0.35)` background) and an "N matches" caption appears above results; an "Add '{query}' as a new customer" row is now always appended after real results (previously that action only appeared in the zero-results empty state, which is unchanged).
  - **Midnight date-rollover bug fix** (`EntryFormScreen`) — the New Entry form's date field defaulted to "today" once via `useState(todayDate())` at mount and was never refreshed. If the form was left open across midnight (backgrounded mid-bill, resumed the next day), it would silently keep submitting against the previous day. Fixed with an `AppState` "active" listener (same pattern as `App.tsx`'s relock logic) that bumps the date forward on resume — but only when the field still holds the last auto-filled value (i.e. the user hasn't manually picked a date) and only in create mode, never edit mode.
  - **Responsive pass** (phone-size + accessibility font-scaling, user-scoped): `Avatar`'s initials, and the three numeric-keypad components (`CalculatorKeypad`, Onboarding's PIN keypad, `LockScreen`'s PIN keypad) now cap `maxFontSizeMultiplier` (1.2–1.3) so large accessibility text settings can't overflow their small fixed-size containers. `LockScreen`'s keypad keys changed from a fixed 68×68 to `flex:1`/`aspectRatio:1`/`maxWidth:84` (capped, centered row) so they scale down on narrow phones instead of a hardcoded size, and up to a sane cap on wider screens. `ReceivableVsCollectedChart`'s bars changed from a fixed 28px width to `flex:1`/`maxWidth:28` within a `flex:1` group, so the weekly chart scales with the card's actual width instead of a fixed size regardless of container.
  - **Google Drive backup** — explicitly out of scope for this pass per user decision: a real OAuth-connected Drive integration would need a Google Cloud OAuth client ID the user would have to provision, a new dependency (`expo-auth-session`) requiring a dev-client rebuild, and is unverifiable given the device install restrictions in `project_device_adb_restrictions` memory; also broader than this project's documented "no cloud sync" v1 scope (`code-style.md`). Backup/restore stays exactly as built in Phase 6 (local JSON export/share).
  - Verification: `npx tsc --noEmit` clean, `npx expo-doctor` 20/20, `npx expo export --platform android` bundles clean.
- [x] **New Bill redesign + standalone Add Items screen** (2026-07-28) — user-requested pass matching `design/11`, `11a`, `11a-1`, `11d`, `11e`, `11f`, not a spec story:
  - **New Bill** (`EntryFormScreen.tsx`, mode `"bill"`) rebuilt to match `design/11`: a read-only, non-persisted "Bill Number" (sequential count of this customer's bills, computed via `listEntriesForCustomer` — there's no such column on `entries`) beside Date; an "Add Items" card (tap the header row, or any committed item row, to navigate to `AddItemsScreen`) showing an ITEM/Qty/Rate/Amount summary table; an Items-subtotal/Invoice-amount totals box (both show the same number — no discount/tax field exists in the data model, same as the existing PDF bill layout); a standalone full-width "Attach receipt" button (previously paired with Date in one row). Line-item editing itself moved off this screen entirely.
  - **New `AddItemsScreen.tsx`** (`design/11a`/`11a-1`) — the line-item editor (one line open at a time, others collapsed to a tappable summary row, "Add another item", "Add to bill · {amount}" footer) that used to live inline on New Bill. State hand-off between the two screens goes through navigation params, not a return value: `EntryForm` → `AddItems` passes the current items as `itemsPayload` (`JSON.stringify(BillLineItemState[])`); `AddItems`'s back arrow and its "Add to bill" button both call `navigation.navigate("EntryForm", { ..., itemsPayload })`, which — since `EntryForm` is already mounted in the stack — pops back to it and merges the new `itemsPayload` into its existing route params (same params-merge pattern already used elsewhere, e.g. Home's stat-tap filters into `CustomerListScreen`); `EntryFormScreen` has a `useEffect` keyed on `route.params.itemsPayload` that applies it when it changes. Tapping a specific item row (on either screen) passes `activeKey` so that item reopens already-active instead of a blank new line.
  - **Category field** (`design/11d`) — a new per-item "Category" dropdown (`BillLineItemCard.tsx`: `CATEGORY_PRESETS` — T-Shirt/Shirt/Kurta/Dress Pants/Jeans/Dupatta/Kids Wear, each with an Ionicons icon and a Named vs Numeric `sizeMode`) with the same searchable-dropdown-plus-freeform-add pattern as Colour below. **Deliberately not persisted** — `category` lives only on the client-side `BillLineItemState`, never sent to `createEntry`/`updateEntry` and never added to the `line_items` schema (a real column would have needed a migration, audit-diff changes, and a data-model.md update for something that's purely a UX shortcut: picking the field's icon and which size list — named S/M/L/XL/XXL or numeric 30–42" waist, per the "Numeric"/"Named" badge next to the Size label — applies). A consequence of this choice: reopening a saved bill's line item for edit never re-shows which category was picked; the size list just defaults back to Named. Selecting "Custom" size still shows the bordered "Enter size" free-text box from `design/11f` (redesigned from the old plain inline text field, same underlying `isCustomSize` state).
  - **Colour field redesigned** (`design/11e`) — replaced the always-visible 14-dot swatch grid with the same closed-box/open-dropdown pattern as Category (search box, "Common colours" chips for all 14 `GARMENT_COLOR_LABELS`, plus "Add '{text}' as new colour"). Unlike Category, this **is** persisted (`line_items.color` was already a plain `text` column, so no migration was needed) — see `data-model.md`'s Garment color palette section for the full reasoning: this is a deliberate, user-confirmed override of that doc's previous "must be one of the 14 labels" rule. A custom color has no hex mapping, so `swatchColorFor()` (new export from `BillLineItemCard.tsx`) falls back to a deterministic hash-to-HSL color for its dot — wired into the Add Items editor, its collapsed rows, and the New Bill summary table, but *not* into `BillSavedScreen`/`EntryDetailScreen`/`EntryHistoryScreen`/the PDF builders in `documentFormat.ts`, which still do a direct fixed-palette lookup and simply omit the swatch for a custom color (pre-existing graceful-degradation behavior, left alone per the "don't touch anything else" scope of this pass).
  - **Description field simplified** — the always-visible separate "Description" `TextInput` (redundant with the auto-caption shown right below it) was removed from the card; per `design/11a-1`/`11f` only the "Auto: '...' · tap to edit" caption remains, and tapping it now actually opens an inline edit field (previously that caption was decorative — a real gap this pass closed). Once a description is manually edited (`descriptionTouched`), a small "Reset to auto" action reverts it. The underlying `generateLineItemDescription()` format was `{qty}x {color} {itemName} ({size})` (spec 7.3) at the time of this pass — **since changed, see the 2026-08-06 entry below** — category does not appear in it, even for a numeric-size item (`design/11a`'s "(Waist 34)" in the caption is the mockup's own copy, not reproduced here, to avoid changing the spec-pinned description format for a decorative label).
  - Docs updated to match: `ui.md`'s New Bill screen-table row rewritten; `data-model.md`'s `line_items.color`/Garment color palette section rewritten for the freeform-color decision; `code-style.md`'s `BillLineItemCard.tsx` example fixed to say it's used by `AddItemsScreen`, not `EntryFormScreen`.
  - Verification: `npx tsc --noEmit` clean, `npx expo export --platform android` bundles clean. No on-device pass (see `project_device_adb_restrictions` memory — still an open gap, unrelated to this change).
- [x] **Auto-description "x" dropped, default line-item category/name, custom-colour name matching** (2026-08-06) — user-requested, not a spec story:
  - **`generateLineItemDescription()` format changed** from `{quantity}x {color} {item_name} ({size})` to `{quantity} {color} {item_name} ({size})` (spec 7.3, `description.ts`) — dropped the leading "x" after quantity app-wide, per explicit user direction to change it everywhere the text is shown (Add Items auto-caption, Entry detail, Khata entries, Bill Saved, the PDF item rows, and the WhatsApp/SMS share text all read from the same `item.description`, so one generator change covers all of them). `data-model.md`'s spec-7.3 section updated to match.
  - **New line item defaults to category "Dress Pants" + item name "Dress Pants"** (`buildInitialLineItem()`, `BillLineItemCard.tsx`) instead of blank — a user-specific convenience since this shop's items are overwhelmingly dress pants; both fields stay fully editable. This turned up a real edge case: `AddItemsScreen`'s `isLineFilled()` previously only checked for a non-empty item name to decide whether to silently commit the active line when navigating away — with a non-empty default name, that alone would now be true from the moment the screen opens, so a truly untouched line (rate still 0) would get committed as a phantom Rs 0 row. Fixed by also requiring `rate > 0` in `isLineFilled()`, matching the real validation `EntryFormScreen.handleSaveBill()` already enforces before an actual save.
  - **Custom colour name matching** — `swatchColorFor()` (`BillLineItemCard.tsx`) now resolves a freeform colour in three tiers: the 14-label `GARMENT_COLORS` palette (exact case), then a new hand-rolled `CSS_NAMED_COLORS` table (~147 standard CSS/X11 names, case-insensitive — "blue" now renders real blue instead of a hashed color), then the existing hash-to-HSL fallback for anything still unrecognized. The "Add '{text}' as new colour" row's preview dot uses the same resolution. Custom colours already used elsewhere in the *same* bill are also collected from the in-progress `lineItems` (`customColorsInUse()`, `AddItemsScreen.tsx`) and offered back as "Used in this bill" quick-select chips in the colour dropdown — derived from `lineItems` on every render, so it survives navigating away to New Bill and back into Add Items, but isn't persisted beyond the bill being built. `data-model.md`'s Garment color palette section updated to describe all three tiers.
  - Verification: `npx tsc --noEmit` clean, `npx expo export --platform android` bundles clean, `npx expo-doctor` — pre-existing SDK patch-version mismatch (5 packages, unrelated to this change) is the only failing check.
- [x] **WhatsApp share now attaches an actual image, not just text** (2026-08-06) — user-reported bug + follow-up request, not a spec story. `share.whatsappHint` (`design/16`) has always read "Send PDF + reminder message", but the WhatsApp option's handler in all three share entry points (`EntryDetailScreen`, `BillSavedScreen`, `CustomerKhataScreen`) only ever called `shareViaWhatsApp()` with plain text — no document was ever generated or attached, contradicting its own copy. First fix pass attached the existing PDF; per direct user follow-up ("send as image... like design/17, directly open that specific number's WhatsApp DM"), superseded by attaching a real **PNG image** instead (previews as a photo bubble in WhatsApp, not a file icon) — see `ui.md`'s Sharing section for the full pipeline (`DocumentReceiptImage.tsx`/`DocumentReceiptImageCapture.tsx`, new `react-native-view-shot` dependency, `lib/share.ts`'s `shareImage()`). Clarified and confirmed with the user via `AskUserQuestion` before building: WhatsApp does not let any app pre-attach media to a specific contact's chat in one tap (deliberate Meta anti-spam restriction, true on both platforms) — only plain text can be pre-filled into a specific chat via `whatsapp://send?phone=...`. So the flow is: generate the receipt/statement as a PNG (off-screen RN render + `react-native-view-shot` capture, not a screenshot of any on-screen UI) → OS share sheet opens with that image → once it closes, WhatsApp opens directly to that customer's chat with the plain-text reminder (`buildBillText`/`buildStatementText`) pre-filled. If capture or sharing fails, the flow stops with `share.unavailable` instead of silently falling back to text-only.
  - **New native dependency**: `react-native-view-shot` (`npx expo install`d, SDK-57-compatible version resolved automatically) — needs a dev-client rebuild before it can run at all; the standalone "PDF" option (`sharePdf()`, unchanged) still works on the current dev client in the meantime.
  - **Not verified on-device** — same test-device install-restriction blocker as everything else needing a rebuild this phase (`project_device_adb_restrictions` memory). This is implemented and `tsc`/`expo export`/`expo-doctor` clean, but the actual captured image (layout fidelity, no blank/cut-off render) has not been visually confirmed on a real device or WhatsApp chat. Flagging as an open item for whoever next has working device access.
  - Verification: `npx tsc --noEmit` clean, `npx expo export --platform android` bundles clean, `npx expo-doctor` — same pre-existing SDK patch-version mismatch (5 packages, unrelated) as the prior entry is the only failing check.
- [x] **WhatsApp share now sends directly to a specific customer's chat, and the view-shot dev-client rebuild landed** (2026-08-07) — user-reported: tapping the WhatsApp share option crashed with `NativeModules.RNViewShot is undefined` (the previous entry's native dependency had never actually been linked into an installed build), plus a direct follow-up request to make the image land in the right customer's chat automatically "just like DigiKhata does" (a competing khata app the user has installed — `com.androidapp.digikhata`, confirmed present on the test device).
  - **Root cause of the crash**: `react-native-view-shot` was added to `package.json` in the prior entry but the on-device app was never rebuilt (the CNG `android/` folder — gitignored, regenerated via `expo prebuild`, see `architecture.md` — predated the dependency). Fixed by a full `npx expo prebuild --platform android --clean` + `npx expo run:android`.
  - **New capability**: `shareImageToWhatsApp()` (`src/lib/share.ts`) uses a new `react-native-share` dependency's `shareSingle({ social: Social.Whatsapp, whatsAppNumber, url, type, message })` — `whatsAppNumber` isn't in the package's own `.d.ts` (extended locally as `WhatsAppShareSingleOptions`) but its native Android code (`WhatsAppShare.java`) turns it into a `jid` intent extra (`<number>@s.whatsapp.net`), an undocumented but long-standing WhatsApp behavior that several ledger/khata apps rely on for exactly this. All three WhatsApp share call sites (`BillSavedScreen`, `EntryDetailScreen`, `CustomerKhataScreen`) now try this first and fall back to the prior two-step flow (`shareImage()` + `shareViaWhatsApp()`) when there's no phone number on file, WhatsApp isn't installed, the platform is iOS (no equivalent there), or the direct call fails for any reason — see `ui.md`'s Sharing section for the full breakdown. This **supersedes** the prior entry's conclusion that direct-to-contact media sharing wasn't achievable; that was true of WhatsApp's public deep-link scheme specifically, not of this intent-extra trick. `share.whatsappHint` copy updated ("Send image straight to their chat") in both locales.
  - **New dependencies**: `react-native-share` (config plugin registered in `app.json` with `android: ["com.whatsapp", "com.whatsapp.w4b"]`, adding the Android 11+ package-visibility `<queries>` entries needed for `isPackageInstalled()`/targeted intents) and `expo-build-properties` (a peer the plugin needs internally).
  - **Device install blocker hit and resolved**: the rebuilt APK's first install attempt failed with `INSTALL_FAILED_INSUFFICIENT_STORAGE: Failed to override installation location` — the test device's `/data` partition was at 0% free (568MB/104GB). This is a different failure mode than the previously-documented `project_device_adb_restrictions` install-cancel issue — a genuine out-of-space condition, not an OEM automation block. Not something to fix via adb (would mean deleting the user's photos/videos/apps without their say); the user freed space manually and a plain `adb install -r` of the already-built APK succeeded immediately after, no rebuild needed.
  - **Verification**: `npx tsc --noEmit` clean. On-device: relaunched via Metro (USB `adb reverse tcp:8081 tcp:8081`), confirmed the exact bill that previously crashed now renders its detail screen cleanly with no `RNViewShot`/`ReactHost` errors in logcat. The `jid`/direct-send path itself still needs a manual on-device tap-through to fully confirm (which specific chat opens, image attached correctly) — `input tap` is blocked on this test device (`project_device_adb_restrictions` memory), so `am start`/`screencap` can confirm crash-free rendering but not drive the Share→WhatsApp button sequence end-to-end.
  - **Metro/adb-reverse gotcha worth remembering**: `expo start --localhost` on this machine (Node 22) resolved `localhost` to `::1` (IPv6) only, binding Metro off the IPv4 loopback that `adb reverse` actually targets — requests either silently never arrived or, if a second stray Metro process happened to be listening broadly on the same port, got picked up by the *wrong* instance (`unexpected end of stream` on the device side, matching an old unexplained `ECONNRESET` crash in a stale `.metro-run.log`). Fix: run `expo start --host lan` (binds `::`, i.e. all interfaces, still reachable via `adb reverse` over USB) and make sure no second `node` process is holding port 8081 before starting.
- [x] **Persisted custom colors/categories, multi-select sizes, and swatch resolution everywhere** (2026-08-07, feature/7-additems-colors-categories-multisize) — user-requested pass, not a spec story:
  - **Persisted custom colors/categories** — new `custom_options` table (migration `0003_chubby_leopardon.sql`; `kind: "color" | "category"`, `label`, `createdAt`) and `customOptionsRepository.ts` (`listCustomColors`/`listCustomCategories`/`addCustomColor`/`addCustomCategory`, dedup case-insensitive against both the built-in labels and whatever's already persisted — no DB unique constraint, see `data-model.md`). `AddItemsScreen` fetches both lists on mount and calls the `add*` functions whenever "Add '{text}' as new colour/category" is taken, updating local state immediately so the new chip is offered without a re-fetch. `BillLineItemCard.tsx` gained a "Your colors"/"Your categories" chip section in each dropdown (new `persistedColorOptions`/`persistedCategoryOptions` props + `onAddCustomColor`/`onAddCustomCategory` callbacks), distinct from the existing bill-scoped "Used in this bill" colour chips — a freeform value typed once is now offered back on every future bill, app-wide, not just for the rest of the bill being edited. Deliberately not audit-logged (`logAudit()`) — this is a picker-convenience list, not ledger history.
  - **Swatch resolution unified app-wide** — `swatchColorFor()` (3-tier: 14-label palette → CSS/X11 named colour → hashed fallback) moved out of `BillLineItemCard.tsx` into `src/lib/garmentColor.ts`, so `lib/documentFormat.ts` can use it without a `lib/` → `screens/` dependency inversion. `documentFormat.ts`'s `swatchHex()` (used by both the PDF builder and, via re-export, `DocumentReceiptImage.tsx`'s WhatsApp image renderer) now delegates to it instead of doing a direct `GARMENT_COLORS[color]` lookup, and `EntryDetailScreen`/`BillSavedScreen` were switched from the same direct lookup to `swatchColorFor()` too. This closes the gap the previous `data-model.md` revision had flagged as future work: a freeform/custom colour like "red" now renders its real swatch everywhere a line item appears — the Add Items editor, New Bill summary table, Entry detail, Bill saved, the PDF, and the WhatsApp receipt image — not only in the editor.
  - **Multi-select sizes** — `BillLineItemCard.tsx`'s size chips are now a toggle (tap to add/remove) instead of single-select, covering a real garment-shop pattern: one line item as an assorted-size batch (e.g. "10 Dress Pants, sizes 32/34/36") instead of three separate lines. Stored as a comma-joined string with no spaces on the existing `line_items.size` column (`"32,34,36"`, always in the preset list's fixed order regardless of tap order) — no schema change needed. `generateLineItemDescription()` needed no change either — it already interpolates `size` as-is, so this produces `"10 Dress Pants (32,34,36)"` for free, matching the requested format exactly. `EntryFormScreen.tsx`'s `lineItemStateFromModel()` (which decides whether a *reopened* saved size looks like a preset or a custom one) was widened to check the union of named + numeric preset lists instead of only named, so a saved numeric or multi-select size doesn't unnecessarily fall into the custom-size box on re-edit.
  - Docs updated to match: `data-model.md` (new `custom_options` table, rewritten Garment color palette section, new Multi-select sizes section), `ui.md`'s New Bill/Add Items row, `architecture.md`'s `lib/` folder listing (added `garmentColor.ts`).
  - Verification: `npx tsc --noEmit` clean, `npx drizzle-kit generate` produced the expected single-table migration, `npx expo export --platform android` bundles clean. No on-device pass (see `project_device_adb_restrictions` memory — still an open gap, unrelated to this change).


## Notes for whoever picks up the next phase
- Read `.claude/docs/architecture.md`, `data-model.md`, and `ui.md` first — they cover the non-negotiable rules (integer paisa, recomputed balance, audit log on every mutation, garment color palette) that every phase depends on.
- The document/export layer (Phase 5) is the model for any future "produce a file" feature: a repository assembler (`getBillDocumentData` / `getStatementDocumentData` / `getExportData`) gathers data, a pure `src/lib/*` builder turns it into HTML/text/CSV/xlsx, and `share.ts` / `exportFile.ts` handle print + share + file writing. Keep data assembly in repositories and formatting in `lib/` (no `db` import in `lib/`).
- `expo-file-system`'s classic API (`writeAsStringAsync`, `cacheDirectory`, base64/utf8 encoding) is imported from `expo-file-system/legacy` in SDK 57 — the default entry is the new `File`/`Directory` API. `src/lib/backupFile.ts` (Phase 6) uses the same legacy import, plus its Android-only `StorageAccessFramework` namespace for restore.
- `updateEntry`/`deleteEntry` (`entryRepository.ts`, Phase 4) are the pattern to follow for any further entry-level mutation: before/patch/diff/`logAudit`, with soft delete only (`isDeleted`, never a real `DELETE` on `entries`). Line items are the one exception — they have no `isDeleted` column, so `updateEntry` hard-deletes/inserts/updates `line_items` rows directly while still logging a `create`/`edit`/`delete` audit row per line, matched by `id`.
- Every screen/component now reads colors via `useTheme()` (`src/theme/ThemeContext.tsx`) and builds its `StyleSheet` with a `makeStyles(colors: AppColors)` factory called through `useMemo(() => makeStyles(colors), [colors])` — never the old `const styles = StyleSheet.create({...})` module-level pattern, which would bake in light-mode colors permanently. Follow this for any new screen (Phase 7's polish pass touches every screen, so it'll hit this constantly).
- Phase 7 is a good place to revisit: a real cross-platform file picker for restore (`expo-document-picker`, needs a dev-client rebuild), an `expo-updates`-based reload after a language switch instead of the manual-restart alert, and dropping the now-dead `settings.pinHash` column in a follow-up migration. None of these three were picked up this pass — still open.
- Phase 7's code-level polish (error handling, empty states, accessibility, large-number formatting, archive/soft-delete UX gaps, icons, `eas.json`) is done — see the checklist above. What's left to actually close the phase: run and QA a real EAS build, and a manual on-device pass against every AC in spec Section 8 (a human tester is needed — this repo's test device blocks `adb` input injection, see the `project_device_adb_restrictions` memory).
